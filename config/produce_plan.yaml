defaults:
  date: "{{today}}"

tasks:
  - section_code: S.STATS.TOP_AFRICAN_PLAYERS
    args:
      - --news
      - curated/news/bbc_football/premier_league/{{today}}/items.json
      - --news
      - curated/news/guardian_football/premier_league/{{today}}/items.json
      - --news
      - curated/news/sky_sports_premier_league/premier_league/{{today}}/items.json
      - --news
      - curated/news/independent_football/premier_league/{{today}}/items.json

# Disabled tasks (saknar runner just nu):
# - section_code: S.NEWS.TOP3
# - section_code: S.NEWS.CLUB_HIGHLIGHT
